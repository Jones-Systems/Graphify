PERSIST-NEEDED — no write/bash tool in inventory. Persist the following markdown verbatim to /home/malcolmjones/Projects/Graphify/research/synthesis/S-D-t11-t14.md

S-D DONE recs=10 headline=Federate-don't-merge on stable CURIE identities with churn-proportional CAS refresh, budget-ledger packing, and HTTP-shaped freshness envelopes — zero new daemons, floor-safe.

```markdown
# S-D Synthesis — Themes T11–T14 (Federation · Freshness · Context Efficiency · Obsidian Ecosystem)

Date: 2026-08-25 · Host: Debian 13.6 trixie VPS, 16-core EPYC Genoa, no GPU, SwapTotal=0 (floor breach ⇒ kernel OOM, not swap thrash) · Python 3.13 · graphifyy==0.9.16 · MemAvailable ≥3072 MiB during bursts · offline-preferred; no cloud keys without owner flag.
Inputs: 22 raw files read in full — L69–L87 (minus L68/L88/L91 which produced none), plus L89, L90, L93 (L92/L94–L96 absent; T14 rests on L93 alone). Note: research/raw/L70-entity-hub-corpus.md holds the research manifest (mis-persisted) while L74-entity-hub-federation.md carries LANE L70's actual entity-hub artifact; cited accordingly.

## 1. Consolidated top-10 recommendations (deduped)

|Rank|Recommendation|Source lanes|Impact0-5|AdoptionEffort(S/M/L)|Confidence|Dependencies|
|---|---|---|---|---|---|---|
|1|Deterministic federated node identity at load layer: composite `(corpus_id, logical_key)` with CURIE display `corpus:relative/path#slug@h8`, integer surrogate PKs, decomposable component columns + `id_aliases(old,new,reason)` table day 1. Fixes live defect: run-timestamped staging-path slugs mint a disjoint ID space on EVERY rebuild (sampled ID embeds `runs_20260825t090340z_full_staging_`)|L69,L73|5|S-M|H|#2/#4 stores; none upstream|
|2|Content-addressed incremental refresh: git plumbing change detection (`diff --name-status -M --diff-filter=ACDMRT` ∪ `status --porcelain=v1 -uall`) against a stored reachability-checked base SHA (full-rebuild fallback); two-tier CAS (sqlite WITHOUT ROWID keyed `ns‖BLAKE3(bytes)`, free git blob OIDs where tracked) + chunk-sha embedding memoization (expected 97–99.5% hit); tantivy per-chunk delete-by-term+add single-commit upsert; versioned-namespace invalidation (`schema‖grammar‖chunker‖er`); stat-screen prefilter; 14-generation GC; 0.5% audit resample; nightly full-rebuild drift backstop; changed-symbol-set reconciliation kills stale cross-file edges|L78,L81,L80|5|M|H|blake3 1.0.9; tantivy 0.26.x; slice governance (§2)|
|3|Cross-corpus weighted RRF (k=60) over bounded-parallel deadline scatter: asyncio TaskGroup + Semaphore(min(16,N)); per-corpus soft timeout `min(0.5·budget, 3·p95)` returning PARTIAL results; uniform oversampled windows `max(100, 3·k_final)`; deterministic-ID dedup BEFORE fusion; p95-triggered hedged re-issue (idempotent reads only); interim SQLite ATTACH UNION-ALL per-corpus LIMIT windows → PG17 UNION ALL when central DB lands|L72|5|M|H|#1 IDs; L17 stdlib fuse|
|4|Federate-don't-merge posture, mechanically enforced: per-corpus graphs stay canonical; union view = `graphify global add <corpus>/current/graph.json --as <corpus>` per promoted run (free tag:: namespacing, label-deduped shared externals, atomic per-corpus refresh, sha256 skip-if-unchanged) under `GRAPHIFY_MAX_GRAPH_BYTES=2GB`; any pair-merge gated on B1–B4 clearance AND ≥100-query probe-set nDCG win ≥5%; STARTS-style meta-layer (size/centroid/top-terms) for routing summaries|L76,L71|4|S|H|#1|
|5|Two-tier classifier-lite corpus router with fan-out fallback: L0 entity/gazetteer df-tables (built during existing index pass) + L1 model2vec potion-base-8M centroid-cosine margins (~30 MB MIT, sub-ms); weighted score → margin rule δ≈0.12 (RouteLLM-style calibration); below-margin → fan out into rec 3; FrugalGPT θ_low post-retrieval escalation (lexical→dense→graph); explicit `repo:` override > classifier > default|L77,L76|4|M|H|model2vec cache; #3|
|6|Entity-hub statement store for people/orgs/projects: SQLite claim-store in FollowTheMoney shape — UUIDv5 (stdlib, RFC 9562) per-corpus namespaces, typed links `(hub_entity ↔ repo-local id)` with confidence+provenance, claim ranks with REVIEW-GATED merges (append-only supersede, never delete), outbox+cursor sync; tame hub-seeded federated PPR with degree damping + contrastive global prior so mega-hubs don't swallow relevance|L74*,L69|4|M|H|#1; Splink/GLiNER output from S-B|
|7|Token-budget-ledger context packing: hard `max_context_tokens` ledger (tiktoken 0.14.0, chars/4 calibrated on our chunks, reserves deducted first) wrapping a score-per-token best-first frontier over graphifyy neighbors (Chakrabarti focused-crawl pattern); PPR distance decay REPLACES hop caps (HippoRAG 2 insight); G-Retriever PCST prize-minus-cost truncation; PathRAG flow pruning (α=0.8, θ-prune, top-K paths; published −40.41% tokens at parity); GraphRAG-class proportions ≈50% snippets/15% summaries/35% entity-edge lines; strongest-evidence-last (lost-in-the-middle); Adaptive-RAG port sets per-query tier D∈{1..3}/B∈{4k,8k,16k}; TALE per-leg floors prevent degenerate stubs|L86,L85|5|M|H|#3 seeds; tiktoken|
|8|Precomputed extractive primer briefs as post-build hook: `brief_builder.py` consumes validated graph.json → leidenalg 0.12.0 RBConfiguration partition (fixed seed, warm-start via `is_membership_fixed`, Jaccard-churn <0.7 re-sweep gate; BSD fallback = networkx louvain/LPA) → Tier-A `corpus-primer.md` ≤3,000 tokens (llms.txt-v2 shape, sha-stamped, PageRank-weighted community directory) + Tier-B per-community detail files fetched on demand|L87|3|S|M-H|#2 rev tokens gate regen|
|9|HTTP-cache-shaped freshness envelope on every response: `{state: valid\|stale\|unknown, as_of, age_s, max_age_s, revalidate_after_s, rev:{git_sha, tantivy_generation, index_build_counter}}` (~40 LOC stdlib); three states with Consul fail-visible rule (absent signal ⇒ unknown, never valid); stale = serve results + async reindex (RFC 5861 stale-while-revalidate); `if_fresh_rev` client echo returns freshness-block-only 304-analog skipping BM25/vector/PPR; recency-decay multiplier kept orthogonal (score axis ≠ truth axis)|L84|4|S|H|#2 manifests rev tokens|
|10|Prompt-cache-friendly deterministic serializer + telemetry: schema-fixed field order per kind, scores quantized (≥3dp/tier buckets) BEFORE sort+emit, rows grouped by kind sorted by stable ID (not score-desc), static preamble first / volatile meta last, no timestamps in body; call sites set Anthropic `cache_control` breakpoints (reads 0.1×, 5-min writes 1.25×) / OpenAI `prompt_cache_key` (128-tok exact-prefix blocks, up to ~90% off cached input); log `cached_tokens` counters per query shape, alert on hit-rate regression|L90|4|S|H|#1 IDs; hosted-LLM consumers|

## 2. Architecture (exact tools + versions for this host)

One deterministic spine: a stdlib loader maps each `<corpus>/current/graph.json` (graphifyy==0.9.16, NetworkX node-link) to `(corpus, relpath#slug)` composite keys + integer surrogates + `id_aliases`; per-corpus stores stay canonical while `graphify global add --as <corpus>` builds the tag-namespaced union (`~/.graphify/global-graph.json`, `GRAPHIFY_MAX_GRAPH_BYTES=2GB`, mem_guard warn<4096/halt<3072 MiB). Query path: asyncio TaskGroup scatter (Semaphore(16), soft timeouts → partials) over per-corpus tantivy probes + sqlite-vec 0.1.9 + FTS5, deduped by CURIE key, fused by weighted RRF k=60 (existing stdlib fuse); router = FTS5 gazetteer + potion-base-8M centroids (δ=0.12, fan-out fallback). Entity hub: SQLite claim-store (FollowTheMoney schema, `uuid.uuid5`, review queue); degree-damped hub priors inside PPR. Refresh runs as `kg-refresh.service` (Type=oneshot, Nice=19, CPUSchedulingPolicy=idle, IOSchedulingClass=idle, OOMScoreAdjust=500, Slice=batch-refresh.slice with MemoryHigh=8G/MemoryMax=12G/CPUWeight=20/CPUQuota=600%) triggered by `.timer` (OnCalendar `*-*-* 03:30`, Persistent=true, RandomizedDelaySec=45m FixedRandomDelay) plus manual `systemctl start`; ExecStartPre `gate-memavailable` exits 75/EX_TEMPFAIL (Restart=on-failure retries); per-stage floor guards + flock single-writer; change detection = git plumbing ∪ porcelain + blake3==1.0.9 (CC0/Apache) or free git blob OIDs; CAS in sqlite WITHOUT ROWID; tantivy 0.26.x per-chunk atomic upsert; NO inotify-driven indexing (watchdog 6.0.0 silently discards IN_Q_OVERFLOW — source-verified). Serving is warm-resident: long-lived tantivy IndexReader (ReloadPolicy::OnCommitWithDelay), persistent sqlite conns, preloaded graph, optional vmtouch pinning behind the floor guard. Responses carry the L84 freshness block LAST inside bodies emitted by the L90 serializer (quantized scores, kind-grouped rows); context assembly runs the L86 ledger around PPR-expanded neighborhoods; the leidenalg 0.12.0 briefs hook (GPL-3 internal-tooling exception, documented; BSD networkx fallback) regenerates only on rev change. Front-matter plane: python-frontmatter 1.3.0 + sqlite-utils 4.2.1 upsert a flattened `note_meta(repo, path PK, mtime, tags_json, projected typed cols)` INTO THE SAME .db as sqlite-vec with FTS5 enabled — feeding RRF pre-filter legs, PPR node attributes, and router gazetteers. Provenance rides free: one factored PROV-O-mappable record per run (reusing ENG-C11 source_fingerprint + ENG-C12 immutable run dirs) plus ~25 B/edge attrs `{r,c,(g)}` in graph.json; `prov==3.1.0` (MIT, zero core deps) for interop export only.

## 3. Rejected / excluded

- merge-graphs single-artifact federation as DEFAULT — est. 330–360 MB output vs 512 MiB per-file load cap (thin margin; ~1.5–2× corpus growth crosses it) and 5–12 s cold reload per CLI invocation; keep only for explicitly requested static snapshots (L71).
- merge-driver / git-merge refresh automation — hard caps `_MERGE_MAX_NODES=100k`/`_MERGE_MAX_BYTES=50MB` abort at our measured 196k nodes/300k edges (L71).
- Merged-global indexes across the 18 corpora — hard blockers B1 (incompatible vector spaces), B2 (ACL/license boundaries), B3 (rolling deterministic-ID rebuild cadence), B4 (heterogeneous schemas) all presently TRUE; BEIR: merged mixed-domain spaces poison off-topic queries (DPR BioASQ nDCG 0.127 vs BM25 0.465); GraphRAG title+type dedup conflates entities (issue #1718 closed not-planned) (L76).
- Elasticsearch / OpenSearch / Meilisearch / Milvus / Weaviate engines — ES RRF gated behind Platinum/Enterprise; OpenSearch JVM heap 1–2 GB+ resident; Meili BUSL-EE split + daemon RAM; evidence-only, cite semantics not binaries (L72,L76).
- ranx as runtime dep — numba/pandas/scipy/seaborn/fastparquet chain; reserve for OFFLINE fusion experiments only (L72).
- Sequential fan-out over 18 corpora — latency sums linearly (median ≈ 18·p50); explicit degradation mode only, never default (L72).
- Raw cross-corpus score-merge (min-max/z-score CombSUM) — uncalibrated BM25/vector scales; Bruch TOIS'24 preconditions (calibrated bounded scores + labels) absent; revisit only behind central-PG shared-stats + eval set (L72).
- Content-hash PRIMARY node IDs for mutable sources — every edit/rename mints new IDs and dangles incident edges (~40.5k-edge repair passes); blake3 stays attribute-level / ER-digest only (L69,L73).
- RDF named-graph substrate (pyoxigraph) / Kythe-Glean platform adoption — second query paradigm or JVM/Haskell ops weight for marginal gain; borrow the identity grammar + PROV-O mapping only (L73,L75).
- Wikibase / yente / NATS for the entity hub — RAM footprint vs floor; boring SQLite claim-store wins (L74*).
- inotify/watchdog event-driven indexing as primary refresh — watchdog `InotifyFD.read_events()` filters out IN_Q_OVERFLOW silently (src-verified 2026-08-25); kernel drops events past max_queued_events=16384; watchfiles 1.2.0 surfaces `need_rescan` only partially; demote to optional daytime dirty-marker consumed at rebuild time (L79).
- anacron-gated cron.daily — anacron ABSENT on this host (`test -x /usr/sbin/anacron` fails); jobs skip silently when the box is busy/down at fire time (L79).
- Watchman since-cursor watching — redundant vs `git status` at ~20 small repos (L78).
- github/stack-graphs — archived read-only 2025-09-09; validates our design (blob-ID caching discussion #263) but DO NOT adopt (L78).
- pymerkle 6.1.0 — GPL-3.0-or-later + unmaintained since 2023-08 + append-only-log API wrong shape for filesystem-snapshot trees; ~150 LOC custom fixed-fanout tree instead (L80).
- Bao outboard verification / fastcdc content-defined chunking — deferred until a second host consumes exports / large blobs enter corpora (L80).
- `graphify update --force` as cache invalidator — audited 0.9.16 source: it ONLY overrides the shrink guard (watch.py:660-706); true from-scratch needs `rm -rf cache/ast/v*/`; the documented `extract --force` remedy is a SILENT NO-OP (cli.py:2048-2049 skips unknown flags); immutable offline builds stay the promotion path; JS family bypasses the AST cache and md frontmatter is stripped pre-hash (metadata-only edits never invalidate) (L82).
- Per-step speculative prefetch (SpecAgent arXiv:2510.17925, Speculative Actions arXiv:2510.04371) — Amdahl-bound: ~700–2500 ms model RTT dwarfs ≤0.8 ms NVMe miss; E[save] ≈ zero/negative per step; SQLite io_uring VFS folklore refuted (no released build has it) (L89).
- RAPTOR / ToG-2 iterative KG↔text pruning — require LLM cluster-summarization / LLM-in-the-loop pruning; blocked by no-local-LLM policy (would be requires-owner-approval) (L85,L86).
- cspy RCSP exact solver — stale (v1.0.3, 2023), single-s-t-path shape wrong for multi-seeded expansion, overkill at 10⁴ nodes (L86).
- TOON as default serialization — only −14.5% vs COMPACT JSON (its −42.6% is vs pretty JSON) and independent eval shows a prompt tax (arXiv:2603.03306); tokenizer pilot first if ever (L90).
- Dataview / Datacore (Obsidian-runtime-bound, no headless mode), Emanote (AGPL-3.0 + Nix/Haskell operational weight), zk v0.15.6 (GPL-3.0; custom front-matter keys printable but NOT filterable) as headless metadata engines — the python-frontmatter+sqlite-utils plane covers the need; mdbasequery (MIT, executes real `.base` YAML headless) only if a Node 20+/Bun sidecar is approved (L93).

## 4. Cross-theme synergies

- Identity is the keystone: CURIE keys (#1) simultaneously serve as RRF dedup keys (#3), CAS cache joins (#2), serializer sort order (#10), entity-hub link targets (#6), and brief-regeneration gates (#8) — the same one-property-fits-many role SCIP symbol strings play in S-B.
- Freshness × context efficiency: rev tokens (#9) double as cache keys for briefs (#8) and packing ledgers (#7); `if_fresh_rev` short-circuits skip whole multi-way queries — freshness work directly buys tokens.
- Federation × packing: fused RRF output seeds PPR expansion (#7); scatter partials under timeout degrade gracefully into smaller packed contexts instead of failing the query.
- Router × freshness: recency/activity prior is one of four router signals (#5); envelope state can down-weight stale corpora before fan-out.
- One governance template: L83's batch-refresh.slice + MemAvailable gate + OOM bias (OOMScoreAdjust=500, optional user.slice MemoryMin=4G) is the same recipe as S-B rec 10's enrich-model.slice — one systemd pattern governs refresh AND enrichment bursts on this swap-less host (SwapTotal=0 ⇒ overshoot ends in kernel OOM).
- Entity hub × S-B ER: Splink cluster output lands as hub claims (confidence ladder mirrors S-B's verified>matched>reviewed); GLiNER spans propose hub candidates; the review queue owns gray-band adjudication; degree-damped PPR keeps hub nodes from dominating walk scores.
- Metadata plane × everything: front-matter note_meta columns act as RRF pre-filter legs, PPR node attributes, and router gazetteer terms — one ingest-time parse pays three times.

## 5. Open questions for owner

1. graphifyy re-pin: pinned 0.9.16 trails PyPI latest 0.9.49 (very active, 448k dl/wk); a re-pin changes the cache namespace and would fix the audited `extract --force` no-op bug — decide before refresh automation lands (L71,L82).
2. Central PG17 research DB (L20 direction; compass :5499 already running): collapses federation to UNION ALL windows and unlocks calibrated score-merge via shared BM25 stats — approve as target, or stay SQLite-interim indefinitely? (L72)
3. GPL-3 internal-tooling exception for leidenalg 0.12.0 (offline, non-distributed), or BSD-only networkx louvain/LPA fallback? (L87)
4. Node sidecar tolerance: mdbasequery (MIT) executes real Obsidian-Bases `.base` queries headless but needs Node 20+/Bun/Deno — acceptable, or Python-only constraint? (L93)
5. Hosted-LLM cache wiring: confirm Anthropic/OpenAI remain primary consumers so `cache_control`/`prompt_cache_key` call-site work ships now (up to ~90% input-cost cut on hits). (L90)
6. Sub-hour freshness SLA: if ever required, a watchfiles(1600/50) dirty-marker bolts on; otherwise nightly+manual is the whole design. Working assumption: NOT required. (L79)
7. Worktree farms in corpus scope (Codex-V3-worktrees ~113, Universal-Agents ~27, CAAM ~17…): include or exclude? Changes the churn model and makes content-hash dedup load-bearing (L78).
8. T14 coverage gap: only L93 landed (L92/L94–L96 produced no raw files) — is owner-facing Obsidian vault projection, extended backlink extraction, and front-matter schema governance still wanted as follow-up lanes?
```

(*L74 cites resolve to the entity-hub artifact physically stored in research/raw/L74-entity-hub-federation.md, whose content is LANE L70's payload.)