# S-D — T11–T14 partial synthesis

## Source binding

- Exact raw-input revision: `c4ed36dc8394cc58a69bb553fc0d3f5e05b13ed0`.
- Lane range: L69–L96.
- Content-validated and consumed inputs: 6 of 28 lanes.
- This is a partial synthesis, not a completeness claim.
- Every consumed lane below is bound only to the exact eligible Git selector
  `c4ed36dc8394cc58a69bb553fc0d3f5e05b13ed0:<mapped artifact path>` in
  `research/RANKING.md`; reviewed-input hashes preserve provenance only.

Validated and consumed inputs: L72, L78–L79, L83, L86, and L93.

Excluded inputs: L71, L73, L76–L77, L80, L84–L85, and L89–L91 are inherited
mixed-context blobs that are not public-safe inputs to this candidate. L69,
L70, L74, L81, and L82 are misrouted; L75 and L87 are gaps with unverified
leads; L88 and L92 are missing. L94, L95, and L96 exist at the raw-input
revision, but contain corrupt internal working analysis and no usable canonical
result. L69, L75, and L87 retain only explicitly non-authoritative design
leads.

## Supported synthesis

| Supported statement | Inputs | Boundary |
| --- | --- | --- |
| Federated result aggregation can evaluate weighted rank-space fusion with bounded parallelism, timeouts, quotas, and explicit partial results. | L72 | Corpus routing, score calibration, identifiers, entity hubs, provenance, and any live service topology remain unvalidated. |
| Refresh experiments can use Git-delta mechanics, generic timer or watcher behavior, overflow rescan, and parameterized resource controls. | L78–L79, L83 | No local inventory, cadence, host limit, cost, command, or deployment setting is accepted. L81/L82 remain swapped artifacts. |
| Token-budget-aware traversal is a documented experiment direction. | L86 | Depth, width, scoring, serialization, and token allocation require public-fixture tests. |
| Headless frontmatter query engines are documented candidates. | L93 | Persistence and target-vault claims are not established; L92 is missing and L94–L96 are corrupt. |

## Unsupported or unknown

No conclusion is accepted from the inherited mixed-context lanes. No canonical
result is accepted for the L69 A/B protocol, L70 judge-bias study, entity hub,
edge-provenance standard, partial-update audit, content-addressed cache,
precomputed briefs, summarization cache, vault projection, backlink extension,
hierarchy mapping, or frontmatter governance. The existence of L94–L96 does
not make their corrupt analysis usable evidence.

## Synthesis conclusion

Only bounded aggregation, generic refresh mechanics, token-budgeted traversal,
and frontmatter-query experiments retain public-safe support. A durable
federation, refresh, context-assembly, or vault architecture is not established.
