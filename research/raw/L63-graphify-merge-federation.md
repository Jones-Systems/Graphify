Federation substrate decision for the S-A search stack: (1) PREREQUISITE — fix tools/graphify/offline_extract.py to pass root-relative paths into the pinned extractors so AST node IDs become stable repo-relative ids (extract.py's own post-pass does this in the canonical pipeline; our private-API wrapper bypasses it, baking absolute staging paths + UTC run dirs into every id — observed in all promoted graphs). Without this, merged graphs are non-deterministic per wave and downstream tantivy/sqlite-vec keys keyed on node id churn every rebuild. (2) SUBSTRATE — either `graphify merge-graphs <18 currents> --out` (batch, tag::namespaced, provably collision-free) or the build.py API trio prefix_graph_for_global/distinct_repo_tags/prune_repo_from_graph composed inside our own tooling; both yield a disjoint union (~29k nodes, well under the 512 MiB query-load cap) where PPR (L17/L02 lanes) can run globally and RRF fusion can treat corpus membership as a free facet via node.repo. (3) REFRESH — global add gives weekly per-corpus replacement with unchanged-hash skipping, matching WENG cadence, and is the only sanctioned way to create cross-repo join points (label-matched source_file-less externals). (4) REJECT — merge-driver (git-oriented, base-blind union) while graphs stay outside Git per D4; naive un-namespaced concatenation would silently fuse unrelated entities (#1729 class) and is prohibited by ENG-C7's duplicate-ID fatal class. Cross-repo entity resolution stays the job of the ER layer (Splink/GLiNER/RapidFuzz), not the graph merge.