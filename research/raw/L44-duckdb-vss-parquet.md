# L44 — DuckDB and VSS over Parquet graph snapshots

## Evidence status

- Classification: recovered summary only.
- Recovery source revision: `d1dbac36208b0066fc8907bd9fe9408fc5c51a60`.
- The committed input contains a result summary but not the claimed findings table or its supporting evidence set.
- The canonical lane report is unavailable in committed repository evidence.

## Recovered summary

The input reports that DuckDB can query node and edge Parquet snapshots and cautions against treating its VSS index as the default persisted vector layer. This summary is non-authoritative and is excluded from synthesis until the underlying evidence is re-established.
