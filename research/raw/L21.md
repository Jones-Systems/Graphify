# L21 — T3 hybrid: local reranker survey

(recovered from lane yield)

Let me parse the assignment. I'm L21, a research lane in a 100-lane investigation. My task:

LANE L21 (T3 hybrid): Local rerankers survey — bge-reranker-base/v2-m3, mxbai-rerank, jina-reranker: CPU latency at 100-candidate pools, quality deltas (MS MARCO/BEIR), memory footprint.

Output contract:
1. WRITE full findings to the exact raw path given. Hmm — "the exact raw path given below" but I don't see a path explicitly... The context says "research/raw/" is where I write. Let me check: "Do not modify anything outside research/raw/." The exact raw path "given below" — perhaps it's implied to be something like `research/raw/L21.md`. I should look at the working directory `/home/malcolmjones/Projects/Codex-V3` and find `research/raw/` and see what naming convention other lanes use.

2. Findings table with columns: |Item|Type(tool/repo/strategy/technique)|URL|License|Maturity|StackFit0-5|EffGain0-5|EffectGain0-5|QualGain0-5|AdoptCost0-5(lower=better)|Conf(H/M/L)|KeyEvidence|

3. ≤3-line verdict: top pick, why, integration sketch.

4. Return message ONLY one line: `LANE L<nn> DONE items=<count> headline=<one-line takeaway>`

Wait — there's tension between the yield protocol (structured result) and the per-lane output contract ("Your RETURN MESSAGE must be ONLY one line"). The yield tool requires result.data matching schema {summary, files, architecture}. I think I should:
- Write findings to research/raw/<file>
- Yield structured data with summary being the one-line DONE message or containing the lane info.
- Final chat message must be only the one line? The yield is the terminal result; my final assistant text should be just the line. Actually the instructions say "Your RETURN MESSAGE must be ONLY one line: LANE L21 DONE items=... headline=..."

I'll do both: terminal yield with structured data (summary = the one-liner plus details), and my visible text reply as the single line.

First, let me explore research/raw/ to see existing conventions.

Then do web research on:
- BAAI/bge-reranker-base (XLM-RoBERTa base, ~278M params, cross-encoder)
- BAAI/bge-reranker-v2-m3 (~568M params, based on BGE-M3, multilingual)
- mixedbread mxbai-rerank-base-v1 / large-v1 (based on RoBERTa/German? Actually mxbai-rerank-base-v1 is based on bge-reranker-base fine-tuned; v2 uses Qwen2-0.5B)
- jina-reranker-v2-base-multilingual (~278M), jina-reranker-v1-turbo-en (~38M?), jina-reranker-v1-base-en, jina-reranker-v1-tiny-en

Key facts to gather:
- CPU latency at 100 candidates
- Quality deltas: NDCG@10 on BEIR/MS MARCO
- Memory footprint (params × fp32/fp16/int8)
- Licenses: bge-rerankers are Apache-2.0; mxbai-rerank Apache-2.0; jina rerankers CC-BY-NC for some (v1 tiny/base/turbo were Apache-2.0 actually; v2-base-multilingual CC-BY-NC-4.0). Let me verify: jina-reranker-v1-tiny-en and v1-small-en are Apache-2.0; v1-base-en/turbo-en CC-BY-NC; v2-base-multilingual CC-BY-NC-4.0. Need to check via search.
- ONNX quantization options for CPU speedups.

Also relevant: FlagEmbedding library usage, reranking latency benchmarks from mxbai blog (they published CPU latency comparisons), Jina's blog on rerankers.

Since no GPU, CPU-only latency matters a lot. A 568M model at fp32 on CPU would be slow (~seconds per query for 100 candidates). Quantized int8 ONNX helps.

Plan:
1. Check research/raw directory for conventions (glob).
2. Parallel web searches for each model family.
3. Write file.
4. Yield + one-line return.

Let me start with glob of research/raw and maybe read an example file to match format.

Note: I'm told to parallelize. Let me fire glob + several web_search calls together. But first glob to see naming; independent anyway.

Searches:
1. bge-reranker-v2-m3 CPU latency benchmark
2. mxbai-rerank-large-v1 benchmark NDCG MS MARCO latency
3. jina reranker v2 base multilingual license NDCG benchmark
4. cross-encoder reranking CPU latency 100 candidates ONNX int8

Also maybe check Hugging Face pages directly via read (URL reader): https://huggingface.co/BAAI/bge-reranker-v2-m3, https://huggingface.co/mixedbread-ai/mxbai-rerank-large-v1, https://huggingface.co/jinaai/jina-reranker-v2-base-multilingual. The instruction says prefer `read` for known URLs over web_search. HF model cards are readable.

Let me batch: glob research/raw/*; read HF pages for bge-reranker-base, bge-reranker-v2-m3, mxbai-rerank-large-v1 (or base-v1), jina-reranker-v2-base-multilingual.

Then follow-up searches for latency numbers (mxbai blog has a nice table: "mxbai-rerank-large-v1 achieves SOTA on BEIR" with latency comparisons). Also Jina blog "A Primer on Cross-Encoders" etc.

Memory footprint: params count × bytes. bge-reranker-base: 278M params → ~1.1GB fp32, 0.56GB fp16, 0.28GB int8. v2-m3: 568M → 2.27GB fp32, 1.14GB fp16, 0.57GB int8. mxbai-rerank-large-v1: 435M (RoBERTa-large based) → ~1.74GB fp32. mxbai-rerank-base-v2: 149M? Actually v2 models are Qwen2-0.5B-based → 494M params. Let me verify via HF.

jina-reranker-v2-base-multilingual: 278M. jina-reranker-v1-turbo-en: 37.9M? tiny-en: 32.7M? small-en: 66.9M? Need verification.

Latency: mxbai blog reported on CPU? They had GPU latency mostly. There are community benchmarks: e.g., "Rerankers benchmarks" by mixedbread shows relative speed. For CPU latency at 100 candidates, realistic estimates: cross-encoder base ~280M on modern EPYC core, fp32 ONNX: roughly 20-60ms per pair → 2-6s for 100 pairs single-threaded; with batching/multithreading maybe 0.5-2s. Quantization int8 gives 2-4x. I need grounded numbers though. Search for actual measured numbers.

Known sources:
- mixedbread blog "mxbai-rerank" announcement includes benchmark tables (BEIR avg NDCG@10: mxbai-rerank-large-v1 0.6409? vs bge-reranker-v2-m3 ~0.63?). Their README has a table with scores on BEIR, and latency comparisons vs Cohere/Jina.
- Sentence-transformers docs list cross-encoders with MS MARCO MRR@10: ms-marco-MiniLM-L-6-v2 (22M) MRR@10 39.01 etc. That's MiniLM family — could be included as ultra-fast option.
- FlagEmbedding README: bge-reranker-v2-m3 performance table.
- Jina AI blog: jina-reranker-v2-base-multilingual technical report with benchmark tables (MKQA, BEIR subsets).

CPU-specific reranker benchmarks: 
- "Fast and Accurate Reranking on CPU" type articles. There's a well-known benchmark by qdrant? Or "rerankers" Python package by Ben Clavié (bclavie/rerankers) — useful integration tool. LightOn? 
- Infinity (michaelfeil/infinity) serves embedding+rerankers with ONNX/OpenVINO backends — good integration route for local serving on CPU.
- text-embeddings-inference (TEI, huggingface) supports rerankers on CPU with flash attention? TEI CPU supports reranker models.

Integration sketch: use TEI or Infinity on CPU with ONNX int8 quantized bge-reranker-base/v2-m3, feed top-100 BM25+PPR fused candidates, cap latency by pool size 50-100, batch 32, threads = cores.

Given hard floor MemAvailable ≥ 3072 MiB, model footprint matters: int8 v2-m3 ~600MB weights plus activations — acceptable. bge-reranker-base int8 ~300MB — safer.

Now quality deltas: typical pipeline without reranker: BM25 top-100 → rerank with cross-encoder improves nDCG@10 substantially (+10-20 points on BEIR subset depending). Compare among rerankers: bge-reranker-v2-m3 BEIR avg ~0.60-0.62; mxbai-rerank-large-v1 ~0.64; jina-reranker-v2 ~0.61? Need real numbers.

From mixedbread README (I recall): BEIR avg NDCG@10:
- mxbai-rerank-large-v1: 64.05? 
- mxbai-rerank-base-v1: 62.31?
- bge-reranker-v2-m3: ~62?
- jina-reranker-v1-turbo-en: ?
Their blog table: "mxbai-rerank-large-v1 outperforms Cohere Rerank English (v3?)..." I'll verify with search.

Jina v2 technical report claims: average score across benchmarks beating bge-reranker-v2-m3 slightly.

FlagEmbedding bge-reranker-v2-m3: evaluated on C-MTEB/CMRC etc. There is a table comparing v2-gemma, v2-minicpm-layerwise, v2-m3, v2_llama3_lightweight on MTEB/CMTEB, miracl, etc. Not BEIR directly.

MS MARCO dev MRR@10 for bge-reranker-base: not officially listed; community: ~39-40? Actually bge-reranker-base trained on MS MARCO among others; sentence-transformers cross-encoder ms-marco-MiniLM-L-6-v2 gets 39.01 MRR@10 (tiny). Larger cross-encoders get higher on BEIR.

Time to gather evidence. Plan tool calls:

Batch 1 (parallel):
- glob: research/raw/*
- read: https://huggingface.co/BAAI/bge-reranker-v2-m3
- read: https://huggingface.co/mixedbread-ai/mxbai-rerank-large-v1
- read: https://huggingface.co/jinaai/jina-reranker-v2-base-multilingual

Batch 2:
- read: https://huggingface.co/BAAI/bge-reranker-base
- web_search: mxbai-rerank CPU latency benchmark 100 documents
- web_search: bge-reranker-v2-m3 ONNX int8 CPU latency milliseconds
- web_search: jina-reranker-v1-turbo-en parameters license apache

Batch 3: fill gaps: TEI/infinity reranker support, rerankers lib, latency measurements (maybe from "rerankers" benchmarks by clavié: he posted latency comparisons on CPU?). 

Then write research/raw/L21.md (check naming from glob results).

Let me go.