Graph-server MCP blueprint (from spec evidence): TRANSPORT — stdio for local agents; Streamable HTTP single POST endpoint on VPS behind nginx (set X-Accel-Buffering:no, emit SSE comment keep-alives, validate Origin); no session affinity needed under 2026-07-28 stateless model (_meta carries protocolVersion/clientInfo/clientCapabilities; server/discover advertises versions; cross-call state only via opaque returned handles). SURFACE — resources/templates (application-driven): kg://{repo}/schema (public-cacheable ttlMs), kg://{repo}/snapshot/{ts}, entity docs; subscriptions/listen filter resourceSubscriptions to push notifications/resources/updated when DuckDB/parquet graph snapshots re-index. Tools (model-driven, all readOnlyHint=true except importers): get_schema; resolve_entity(name)->ranked candidates (Splink/RapidFuzz-backed); neighborhood(node_id, depth, limit); ppr_walk(seeds, edge_types, damping, k) over graphifyy KG; hybrid_search(query, top_k) fusing tantivy BM25 + LanceDB arctic-embed vectors + PPR-over-KG; explain_plan for cost visibility. Each returns outputSchema-conforming structuredContent ({nodes[],edges[],paths[],scores[]}) plus mirrored TextContent, cursor pagination, row caps; validation failures as isError:true actionable messages (SEP-1303) so the model self-corrects; deterministic tool ordering for prompt-cache hits. DEFER: OAuth 2.1 resource-server stack (RFC 9728/8707/9207, CIMD, client-credentials ext) — internal VPS uses env/bearer; Tasks extension for >30s PPR runs; MRTR disambiguation flows. AVOID building on: sampling, roots, logging, HTTP+SSE, DCR — all Deprecated with removal eligibility from 2027-07-28.